# Thư mục doc/ CỐ Ý không nằm ở đây nữa (bỏ ngày 04/09/2026): kịch bản demo, bộ ca kiểm thử và các
# tài liệu bàn giao là thứ cả nhóm cần dùng chung, để ngoài repo thì người khác clone về không có.

# Cấu hình riêng của trình soạn thảo trên từng máy
.vscode/
.idea/

# Bí mật thật của từng máy (mật khẩu ứng dụng Gmail...). Spring Boot tự đọc file ./config/
# application.properties bên cạnh chỗ chạy và ĐÈ LÊN file trong resources -- không cần cờ gì thêm.
# Repo này công khai: mật khẩu ứng dụng Gmail cho phép gửi thư VÀ đọc hộp thư qua IMAP dưới danh nghĩa
# chủ tài khoản, nên nó tuyệt đối không được nằm trong lịch sử git.
backend/config/

# File hệ điều hành
Thumbs.db
.DS_Store
